<body>
  <section class="hero">
    <img
      src="img/Screenshot 2025-05-14 at 15.09.35 1.png"
      alt="Vin med Dans hero"
      class="hero-image"
    />
    <div class="hero-content">
      <p class="scroll-text">← SCROLL NED</p>
      <div class="age-badge">+18</div>
    </div>
  </section>
</body>

<style>
  .hero {
    position: relative;
    width: 100%;
    height: 100vh;
    overflow: hidden;
  }

  .hero-image {
    width: 100%;
    height: 100%;
    object-fit: cover;
    position: absolute;
    inset: 0;
    z-index: 0;
  }

  .hero-content {
    position: relative;
    z-index: 1;
    height: 100%;
    display: flex;
    flex-direction: column;
    justify-content: space-between;
    padding: 1rem;
  }

  .scroll-text {
    writing-mode: vertical-rl;
    transform: rotate(180deg);
    font-size: 0.75rem;
    color: white;
    opacity: 0.8;
    align-self: flex-start;
  }

  h1 {
    font-size: 2.5rem;
    text-align: left;
    color: #fff;
    font-family: "YourVinFont", sans-serif;
  }

  .age-badge {
    background: rgba(0, 0, 0, 0.6);
    border-radius: 50%;
    color: white;
    font-size: 0.8rem;
    padding: 0.5rem;
    align-self: flex-end;
  }
</style>
